import { WidthLoadingPipe } from './width-loading.pipe';

describe('WidthLoadingPipe', () => {
  it('create an instance', () => {
    const pipe = new WidthLoadingPipe();
    expect(pipe).toBeTruthy();
  });
});
